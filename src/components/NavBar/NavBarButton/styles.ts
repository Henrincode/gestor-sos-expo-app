import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    // overflow: 'hidden',
    alignItems: 'center',
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 6,
  },
  text: {
    fontWeight: 'bold'
  },
  selected: {
    borderBottomWidth: 2
  }
})

export default styles